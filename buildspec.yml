version: 0.1
env: 
    secrets-manager:
      TOKEN: firstSecret:tokenforsonar
phases:
  build:
    commands:
      - mvn verify sonar:sonar -Dsonar.projectKey=newawscodebuildproject -Dsonar.organization=newawscodebuildproject -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=$TOKEN
